export enum UserType {
  RIDER = 'rider',
  CUSTOMER = 'customer',
  OPERATOR = 'operator',
  ADMIN = 'admin',
}

export enum ChatMemberType {
  RIDER = 'rider',
  CUSTOMER = 'customer',
  VENDOR_LOCATION = 'vendor_location',
}
